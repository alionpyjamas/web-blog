<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Professional Blog</title>
    <style>
        body {
            font-family: 'Roboto', sans-serif;
            background-color: #f0f2f5;
            color: #333;
            margin: 0;
            padding: 0;
        }

        .container {
            max-width: 800px;
            margin: 50px auto;
            background: #ffffff;
            padding: 30px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            border-radius: 12px;
        }

        h3.title {
            font-size: 26px;
            margin-bottom: 10px;
            color: #444;
            transition: color 0.3s ease;
        }

        h3.title a {
            text-decoration: none;
            color: inherit;
        }

        h3.title a:hover {
            color: #1e90ff;
        }

        p.date {
            font-size: 14px;
            color: #999;
            margin-bottom: 20px;
        }

        p.content {
            font-size: 18px;
            color: #666;
            line-height: 1.8;
        }

        hr {
            border: none;
            border-top: 1px solid #eee;
            margin: 40px 0;
        }

        .create {
            display: inline-block;
            padding: 12px 25px;
            margin-top: 20px;
            background-color: #1e90ff;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px;
            transition: background-color 0.3s ease, transform 0.3s ease;
        }

        .create:hover {
            background-color: #0056b3;
            transform: translateY(-2px);
        }

        .recent-post {
            font-size: 22px;
            color: #555;
            margin-top: 30px;
            font-weight: 500;
            text-align: center;
        }

        .container-2 {
            text-align: center;
            margin-top: 60px;
        }

        p {
            text-align: center;
            font-weight: bold;
            font-size: 20px;
            color: #444;
        }

        ul{
            list-style: none;
            padding: 0;
            margin: 0;
        }

        li.para-1, li.para-2 {
            padding: 20px;
            margin-bottom: 20px;
            background: #f8f9fa;
            border-left: 5px solid #1e90ff;
            border-radius: 10px;
            transition: box-shadow 0.3s ease, transform 0.3s ease;
        }

        li.para-1:hover, li.para-2:hover {
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
            transform: translateY(-5px);
        }
    </style>
</head>
<body>
    <%- include('header') %>
    
    <div class="container">
        <% if (posts.length === 0) { %>
            <div class="text-center">
                <h3>Empty here...</h3>
            </div>
        <% } else { %>
            <ul class="1st">
                <% posts.forEach((post) => { %>
                    <li class="para-1">
                        <p class="date"><%= post.date %></p>
                        <h3 class="title"><a href="/viewpost/<%= posts.indexOf(post) %>"><%= post.title %></a></h3>
                        <p class="content"><%= post.content %></p>
                        <hr>
                    </li>
                <% }) %>
            </ul>
        <% } %>
    </div>

    <div class="container-2">
        <a href="/create"><button class="create">Create Post</button></a>
        
        <% if (posts.length === 0) { %>
            <div class="recent-post">Make some posts!</div>
        <% } else { %>
            <div>
                <p>RECENT POSTS</p>
                <ul class="2nd">
                    <% posts.forEach((post) => { %>
                        <li class="para-2">
                            <h3 class="title"><a href="/viewpost/<%= posts.indexOf(post) %>"><%= post.title %></a></h3>
                        </li>
                    <% }) %>
                </ul>
            </div>
        <% } %>
    </div>
</body>
<%- include('footer') %>
</html>
